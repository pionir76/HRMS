# CLAUDE.md

이 파일은 이 프로젝트에서 작업할 때 Claude Code가 따라야 할 규칙을 담고 있습니다.

## Git 작업 규칙

- `git commit`, `git push`는 항상 실행 전에 사용자에게 확인을 받는다. `git add . -> commit -> push`처럼 반복되는 흐름이라도 자동으로 실행하지 않는다.
- 커밋 메시지는 실제 변경 내용을 요약한 설명을 사용한다 ("aaaa" 같은 자리표시자 금지).
- 커밋 메시지 옵션은 `-m` (소문자)을 사용한다. `-M`은 `git branch -M main`처럼 브랜치 강제 지정/rename 용도이며 커밋 메시지 옵션이 아니다.
- `git add .` 실행 후에는 `git status`로 스테이징된 파일을 확인한 뒤 커밋을 진행한다.

## 설계/코드 원칙

- 여러 대안 중 선택할 때, 다소 이상적이지 않거나 손해로 보이더라도 **읽기 쉽고 구조가 단순한 쪽**을 우선한다.
- "이론적으로 더 낫다"는 이유로 복잡한 추상화, 과도한 일반화, 영리한 기법을 도입하지 않는다.
- 이 프로젝트는 소규모(사용자 약 20명) 내부 시스템이므로, 확장성보다 유지보수 용이성과 가독성을 우선한다.
