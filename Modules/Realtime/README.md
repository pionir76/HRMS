# Realtime 모듈

실시간값 관리 모듈.

- 압축기별 최신 센서값을 서버 메모리에 유지 (DB 조회 없이 즉시 제공)
- 압축기별 최신 측정 시각, 통신 상태, 운전 상태, 경보 상태 제공
- 프론트엔드 REST Polling(2~3초 주기)에 대응

## 내부 구성 (예정)

- `Services/` — 스레드세이프 메모리 캐시
- `Controllers/` — 실시간 데이터 조회 API
